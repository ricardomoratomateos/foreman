## Why tmux

Every agent Foreman launches runs inside a tmux session. That is what lets it keep working while you reload the VS Code window, and what lets Foreman find it again afterwards.

If tmux is not installed, the **Worktrees** panel says so and offers to install it with your package manager. Once it is there, reload the window and you are in.
