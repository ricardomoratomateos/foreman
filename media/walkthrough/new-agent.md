## One task, one worktree, one agent

Press **+** in the Worktrees header. Give the task a title, pick the branch to start from, and describe what the agent should do.

Foreman cuts a branch, creates a git worktree for it, and launches your primary agent inside — with your task as its first prompt. The main checkout stays untouched.
