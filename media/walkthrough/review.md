## Review without a PR round-trip

The diff button on a card opens the worktree's changes against its base branch. Comment on any line and send the comments straight to the agent's terminal.

Screenshots work the same way: drop an image on **Screenshot drop** in the sidebar, or press `Cmd+Alt+V` inside the agent's terminal to attach the latest one from your screenshots folder.
