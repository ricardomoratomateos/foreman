## The setup travels with the code

Everything in the **Project** half of settings is saved to `.unmess/config.json`. Commit it: whoever clones the repo gets the same worktree folder, ports, scripts and debugger — no pasting settings around.

The **You** half (which agent you prefer, notifications, focus mode) stays in your own VS Code settings and never enters the repository.
